# 浏览器云端个人桌面 V0.14 Workspace Application 施工计划

状态：施工中

日期：2026-07-31

基线：`v0.13.0`

## 1. 目标

把已冻结的 BWA 概念分成三个可独立验证的纵向切片，不在第一阶段提前实现容器生命周期
和代理，也不在 V0.14 引入 Compose、私有 registry 或多容器能力。

## 2. V0.14-A：Registry、Image 与持久 Schema

### A1 局部设计与评审

状态：已完成

- 冻结 repository/digest 身份；
- 冻结 RefStore v3 Application/Instance/environment Schema；
- 冻结敏感变量载体和故障顺序；
- 冻结 Runtime Image API 权限边界。

### A2 RefStore v3

状态：已完成

- v1/v2 原子迁移到 v3；
- Application、Instance、environment 持久实体；
- Workspace 独占绑定和删除保护；
- 首个 Instance 自动成为 default；
- Instance 解绑时确定性迁移 default；
- 空白 Workspace 与 Instance 单事务发布。

### A3 Runtime Image API

状态：已完成

- `pullAndInspect`；
- `inspectInstalled`；
- GHCR 引用规范化；
- 从本地实际 RepoDigest 固定版本；
- labels、Entrypoint/Cmd 和输出大小限制；
- Unix socket 精确请求 Schema。

### A4 Registry 与敏感变量

状态：已完成

- 镜像协议和展示元信息校验；
- 安装记录、Instance 创建和环境配置服务；
- `0600` 敏感变量原子文件；
- 敏感值缺失时失败关闭；
- orphan secret 清理；
- 重启恢复和 SQLite 不含 secret 的测试。

### A5 阶段出口

状态：已完成

1. 公开最小 BWA 诊断镜像已发布；
2. 真实 Compute Runtime socket 已拉取并固定 digest；
3. `inspectInstalled` 返回相同 digest；
4. Registry、secret、空白 Workspace 和 Instance 闭环已通过验收。

## 3. V0.14-B：Lifecycle、提交与异常恢复

状态：已完成并通过阶段验收

1. 冻结 desired/startup/observed 映射；（已完成）
2. 扩展 Run binding、动态 launch spec 和固定 BWA OCI 计划；（已完成）
3. 实现 start、正常 stop 和提交的第一控制链；（已完成并通过真实 GHCR/PVLogFS/COW 验收）
4. 实现 graceful stop 分类、exit 0 提交和异常 Upper 保留；（已完成，Supervisor 单次巡检已接入）
5. 实现“保存状态并重启”；（已完成并通过真实 Runtime 状态延续验收）
6. 实现宿主恢复、全 Instance 更新门和单次回退；（已完成并通过 daemon 强制中断验收）
7. 通过正常、崩溃、断电恢复和提交失败测试。（已完成）

## 4. V0.14-C：Runtime Proxy、iframe 与产品界面

状态：施工中

1. 冻结私有网络、稳定 Instance URL、认证和 WebSocket；（设计完成；origin/session、bridge endpoint 与 HTTP 代理已施工，真实纵向验收待执行）
2. 定义最小 BWA iframe Protocol v1；
3. 实现 Manager internal 启动、进度、错误和恢复页；
4. 接入设置、应用菜单和桌面入口；
5. 完成安装、首次打开、保存、Fork、更新、回退和卸载人工验收；
6. 更新关于界面、文档索引和里程碑归档。

## 5. 合并门槛

- `npm run build`、`npm run lint` 和完整测试通过；
- Node 24 下 RefStore v1/v2→v3 迁移、备份恢复通过；
- 真实 GHCR 镜像固定和重启检查通过；
- V0.13 Workspace Runtime 回归不退化；
- 不向 Manager 或 iframe 暴露 Docker socket、任意 Docker 参数或 secret 值；
- 三个切片全部验收后合并并标记 `v0.14.0`。
