# 浏览器云端个人桌面 V0.6 文件管理器可用性施工计划

状态：验收完成，等待里程碑合并

## 阶段 0：基线与文档

- [x] 从 `v0.5.0` 建立独立分支；
- [x] 修正 V0.5 已合并归档状态；
- [x] 冻结 V0.6 必要范围和非目标；
- [x] 明确文件复制零拷贝和文件夹复制延后。

## 阶段 1：服务端事务

- [x] `CreateFileInput` 支持 expected revision；
- [x] internal 新建空文件；
- [x] internal 零拷贝复制文件；
- [x] 名称、类型、权限、重名和 revision 校验；
- [x] 单元与 HTTP 边界测试。

## 阶段 2：文件管理器体验

- [x] “新建文件”名称对话框；
- [x] “复制”名称对话框和合理默认名称；
- [x] 操作中锁和成功/失败反馈；
- [x] 主列表导航清除陈旧 listing；
- [x] 移动目标加载反馈；
- [x] 快速重复操作回归测试。
- [x] 工具栏紧凑 SVG 图标、功能分组与可访问名称；
- [x] 工具栏在上、`/` 根面包屑在下；
- [x] 可关闭的操作提示条。

## 阶段 3：验收与归档

- [x] lint、全量测试和生产构建；
- [x] Docker/R2 新建、打开和保存；
- [x] 小文件与 64 MiB+1 文件复制；
- [x] FID 复用证据；
- [x] stale revision 和重名失败；
- [x] 容器重启；
- [x] README、验收记录和归档状态。

## 真实验收记录

2026-07-29 在真实 Docker、R2 和既有持久卷中完成：

- 新建 `未命名.txt`，由 Notepad 打开、保存并重开，内容正确；
- 复制文本文件后原件和副本内容一致，修改副本不影响原件；
- 上传并复制 67,108,865 字节的 `large-boundary.bin`，副本即时出现且大小一致；
- 自动化索引检查证明副本使用新 Entry ID 并复用原 content FID；
- 同名副本被拒绝，改名后只创建一个副本；
- 两个浏览器标签制造 stale revision，旧对话框提交被拒绝且未创建 `stale-test.txt`；
- 容器重启后文本、大文件及副本保持存在，失败操作未产生可见 Entry。
- 紧凑 SVG 工具栏、功能分组、可关闭提示条和 `/ › 目录` 面包屑完成真实界面复验。

详细证据见
[V0.6 文件管理器可用性验收](<acceptance/V0.6 文件管理器可用性验收.md>)。

## 合并门槛

- 不修改 PVLog、Host API 或第三方协议原文；
- 文件复制不读取或重写内容对象；
- 文件夹复制不以非原子循环请求实现；
- internal 权限边界不扩大；
- 失败不发布 Ref；
- 所有自动化和真实环境验收通过。
