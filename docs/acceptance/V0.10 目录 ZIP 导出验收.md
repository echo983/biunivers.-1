# V0.10 目录 ZIP 导出验收

日期：2026-07-30

状态：通过，归档版本候选 `v0.10.0`

## 自动验证

- `npm test`：62 个测试文件通过，1 个测试文件按既有运行条件跳过；263 项通过，1 项跳过；
- `npm run lint`：ESLint 通过；
- `npm run build`：TypeScript、Vite 客户端和 Node 服务端生产构建通过；
- ZIP Store 覆盖 UTF-8 路径、空目录、零字节文件、分块正文、CRC-32、Data Descriptor、
  Central Directory、路径安全、重复路径、ZIP32 上限和正文长度不一致；
- 系统 `unzip -t` 对实际生成物校验通过；
- 快照测试确认导出计划固定后发生内容更新，ZIP 仍读取开始 revision 的旧不可变 Content Ref；
- HTTP 测试覆盖 internal 鉴权、准确 Content-Length、Unicode 下载文件名和分块响应；
- 前端测试覆盖原文件下载、单目录 ZIP 下载、进度和浏览器保存。

## 人工验收

真实 Docker/R2 浏览器环境已完成以下检查，全部符合预期：

1. 单文件下载仍得到原文件；
2. 单个目录下载为 `<目录名>.zip`；
3. ZIP 保留顶层目录、两层以上嵌套目录和空目录；
4. 中文、空格文件名解压正常；
5. Ctrl/Shift 多选文件和目录后下载为 `biunivers-download.zip`；
6. 大于 64 MiB 的文件导出后内容和大小正确；
7. 下载过程中进度更新，取消后不保存文件；
8. 上传、移动、复制和移除无回归。

## 结论

目录快照、不可变内容读取、ZIP Store 流、HTTP 下载、进度、取消、单文件兼容与批量选择路径
均已闭环。V0.10 具备合并和创建 `v0.10.0` 里程碑标签的条件。
