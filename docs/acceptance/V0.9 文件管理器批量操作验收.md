# V0.9 文件管理器批量操作验收

日期：2026-07-29

状态：通过，归档版本 `v0.9.0`

## 交付范围

- 普通点击、Ctrl/Command 切换、Shift 区间和组合追加选择；
- 多选计数以及单项动作禁用；
- 原子批量移动、递归复制和删除；
- 目录复制生成新 Entry ID，文件内容复用原 FID；
- 祖先与后代选择规范化、名称冲突、环路、根目录、数量和 revision 校验；
- 文件删除后的桌面失效联动。

批量接口仅供内建文件管理器使用，不向第三方应用开放目录遍历能力。

## 自动验证

- Rust `pvlog-core`：9 项通过；
- Vitest 完整测试：254 项通过，1 项跳过；
- 文件选择与文件管理器定向回归：15 项通过；
- HTTP 路由定向回归：9 项通过；
- ESLint：通过；
- TypeScript 客户端与服务端生产构建：通过；
- `git diff --check`：通过。

## 真实环境验收

使用现有 Docker、File Service、真实 S3/R2 配置和持久化数据卷完成浏览器验收：

- Ctrl/Command 多选符合预期；
- Shift 连续选择符合预期，且不再触发浏览器文字选择；
- 组合选择与选择计数符合预期；
- 批量移动、复制和删除符合预期；
- 递归目录复制及刷新后的持久状态符合预期；
- 多选时单项动作禁用符合预期。

## 结论

选择模型、UI、internal API、PVLog 原子事务、不可变内容复用和真实持久化形成闭环。
V0.9 已具备合并条件；标签、覆盖/合并策略和第三方文件系统浏览不属于本里程碑。
