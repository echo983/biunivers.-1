# V0.14-A BWA Registry 与 Image 验收

状态：通过

日期：2026-07-31

## 1. 验收对象

- 公开源码仓库：`https://github.com/echo983/biunivers-bwa-diagnostic`
- 公开镜像：`ghcr.io/echo983/biunivers-bwa-diagnostic:latest`
- 诊断仓库提交：`6c1c36da6354e3f4874f70668d74ef3addfa6787`
- GitHub Actions 发布：通过

## 2. 镜像行为

本地真实容器完成：

```text
GET /health
→ POST /api/increment
→ /workspace 写入 count=1
→ 销毁容器
→ 使用同一 Workspace volume 重建
→ GET /api/state 仍返回 count=1
```

镜像以非 root 用户运行，监听 `0.0.0.0:8080`，并带有协议、description、source、
version 和 license OCI labels。

## 3. GHCR 与 Runtime Image API

GHCR 匿名 manifest 请求返回 `HTTP 200`，确认镜像为公开可拉取状态。

验证脚本：

```bash
sg docker -c \
  'PATH=/home/edwin/.nvm/versions/node/v24.18.0/bin:$PATH \
   node scripts/verify-bwa-image-api.mjs'
```

实际调用链：

```text
ComputeRuntimeImageClient
→ 0600 Unix socket + token
→ ComputeRuntimeServer
→ DockerImageAdapter.pullAndInspect
→ docker pull / image inspect
→ DockerImageAdapter.inspectInstalled(repository@digest)
```

固定结果：

```text
repository  ghcr.io/echo983/biunivers-bwa-diagnostic
digest      sha256:b187f2108bd3d83f4b015aa0b59484ae1f2ed702e4f806d5b1991acc5f759908
platform    linux/amd64
protocol    1
```

首次 pull 后从本地实际 RepoDigest 取得身份，按固定 digest 二次检查得到同一结果。

## 4. 自动化回归

- Node 24 完整测试：396 通过，1 跳过；
- V0.14-A 定向测试：36 通过；
- RefStore v1/v2→v3 迁移：通过；
- build：通过；
- lint：通过；
- secret 值不进入 SQLite：通过。

## 5. 结论

V0.14-A 阶段出口全部满足。公开 GHCR 镜像、固定 digest、窄 Runtime Image API、
Application/Instance 持久关系、空白 Workspace 原子创建和敏感变量边界已经形成闭环，
可以进入 V0.14-B Lifecycle、提交与异常恢复局部设计。
