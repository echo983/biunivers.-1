# Biunivers V0.11 Wormhole 验收

状态：Linux/rclone 主路径通过；Windows Explorer、macOS Finder 与公网 HTTPS 环境待补充

日期：2026-07-30

## 自动验证

- TypeScript 与生产构建通过；
- ESLint 通过；
- 全量 Vitest：67 个文件通过、1 个跳过，276 项通过、1 项跳过；
- Wormhole 专项：路径安全、运行时认证、internal 控制权限、不可变文件事务和进程锁共
  13 项通过；
- 现有桌面、File Service、Resource Session 和应用生态回归无新增失败。

## Docker 与界面

真实 Docker 容器使用 R2 File Service 启动成功。用户确认：

- Wormhole internal 应用可打开；
- 开启后生成随机 10 位密码；
- 地址、用户名、密码和命令可复制；
- 换密、关闭与 Sync 方向切换符合预期；
- 非实时同步定位、Windows 说明和危险操作警告可见。

## WebDAV 直接验收

`scripts/verify-wormhole-webdav.sh` 在真实容器/R2 上通过：

- MKCOL、PUT、PROPFIND Depth 1、GET；
- 单 Range、COPY、MOVE、DELETE；
- 测试目录清理。

## rclone 验收

`scripts/verify-wormhole-rclone.sh` 通过列目录、上传、完整读取、Range、服务端 COPY、
MOVE、DELETE 与清理。

`scripts/verify-wormhole-mount.sh` 通过 Linux FUSE 挂载、普通文件系统创建、写入、读取、
改名、删除、卸载与清理。

## 大文件与不可变存储边界

`scripts/verify-wormhole-large-file.sh` 使用 `64 MiB + 1 byte` 文件通过：

- 流式跨分片上传与完整下载；
- 本地逐字节 `cmp`；
- 分片边界 Range 内容校验；
- 测试文件系统引用清理。

这证明 Wormhole 沿用 File Service 的 64 MiB 分片、不可变对象与 Range 读取路径，没有
引入第二套可变文件后端。

## 尚未在当前环境执行

- Windows Explorer 原生 WebDAV CRUD；
- macOS Finder 原生 WebDAV CRUD；
- 公网 HTTPS 反向代理；
- 真实跨设备双向 `rclone sync --interactive`。

这些项目需要对应操作系统或部署环境。Linux/rclone 是 V0.11 的推荐主路径，已完成真实
数据验收；原生客户端结果不得在未实测前标记为通过。
