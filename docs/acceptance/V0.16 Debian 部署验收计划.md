# V0.16 Debian 部署验收计划

状态：KVM 基线已通过，等待真实 Release
日期：2026-08-06

## 已自动通过

- Runtime Release 真实构建、包内 Node 24 和原生模块加载；
- Host/Runtime launcher 与 systemd unit 静态校验；
- 安装器 checksum、metadata、digest、幂等续装和拒绝错误 checksum；
- 更新器成功提交与健康失败回滚的隔离事务；
- GitHub Release workflow 的 tag、权限、Debian 12、双 digest 和匿名拉取门禁；
- Debian 12/13 KVM 夹具 shell、官方镜像 checksum、SSH host key 和 reboot 路径。

## 真实环境待通过

1. Debian 12 干净安装；
2. Debian 12 reboot 恢复；
3. Debian 13 干净安装；
4. Debian 13 reboot 恢复；
5. 安装后的真实 PVLogFS、overlay、非 root 诊断容器和 COW 提交；
6. 已发布 patch Release 的成功更新；
7. 故障 Release 或注入健康失败的自动回滚；
8. 回滚后 RefStore、未决 Upper 和旧版本服务状态核对。

真实项没有通过前，本文件不得改为“已完成”，V0.16 部署能力也不应被描述为已经在干净 Debian
上验收。

## 2026-08-06 KVM 基线证据

已在开发机真实安装 QEMU 10.0.11 并启动两台相互独立的官方 genericcloud VM：

| 目标 | 镜像 SHA-512 | 内核 | 虚拟化 | cloud-init |
| --- | --- | --- | --- | --- |
| Debian 12 | `32df4978ddb883b336d3a08737e55f12a8241d8ca0ca5bc6bf28bfcb37c9475f2f264a0f9119692affeda3f0c75413daa58dc8ca5dd373244c78dbc7c4745288` | `6.1.0-51-cloud-amd64` | KVM | done |
| Debian 13 | `769562604ecaac26b661167891ef922f71f4d87d50a11423fc04e51444fda0d882c87996dd1181170d233627f4728e6722db2695c0ef753dad762c4ac4ed32e1` | `6.12.100+deb13-cloud-amd64` | KVM | done |

两台 VM 均通过官方同目录 `SHA512SUMS`、SSH host key、cloud-init 等待和 32 GiB qcow2 overlay
检查。VM 当前保持运行，分别监听宿主 loopback SSH 端口 2212、2213。

GHCR 尚不存在可匿名拉取的 `biunivers` 与 `biunivers-runtime-diagnostic` 正式包，因此没有伪造
本地 tag 绕过 digest 门禁。下一步应先完成分支评审、合并和首次稳定 Release，再把同一 Release
资产投入这两台 VM；若安装失败，以 patch Release 修复并保留失败证据。
