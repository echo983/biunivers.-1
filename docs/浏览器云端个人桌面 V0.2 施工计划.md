# 浏览器云端个人桌面 V0.2 施工计划

状态：施工完成，待里程碑合并

目标：交付第三方静态应用安装、托管和管理闭环

## 1. 施工原则

- 每个阶段都产生可验证结果；
- 优先打通纵向链路，再补充生命周期；
- 服务端规则必须有自动化测试；
- 不执行第三方仓库代码；
- 不把 future protocol 能力混入 V0.2；
- 发现协议问题时同步修改协议、Schema、模板和实现；
- 未通过阶段出口条件，不提前宣称功能完成。

## 2. 阶段总览

| 阶段 | 内容 | 主要产物 |
|---|---|---|
| 0 | 设计基线 | V0.2 需求、技术设计、施工计划 |
| 1 | 服务端与持久化骨架 | 双 origin、管理认证、状态文件和 Docker 基础 |
| 2 | 注册表边界收紧 | internal 白名单、多来源 APP 合并和降级 |
| 3 | 检查与首次安装 | GitHub source、Manifest/协议校验、文件落盘 |
| 4 | 桌面应用管理 UI | 解锁、检查、动态配置、安装和列表 |
| 5 | 生命周期 | 配置、更新、停用、启用和卸载 |
| 6 | 集成与发布准备 | Docker E2E、文档同步、协议定稿判断 |

## 3. 阶段 0：设计基线

### 工作

- [x] 编写 V0.2 需求方案；
- [x] 编写 V0.2 技术设计；
- [x] 编写 V0.2 施工计划；
- [x] 映射 Static App Protocol、Manifest 和 Management Protocol；
- [x] 评审并确认 V0.2 范围；
- [x] 提交和推送设计基线。

### 出口条件

- 核心安装流程有明确输入、状态和失败行为；
- 技术架构能解释写入、托管、认证和 origin 隔离；
- 每个协议要求有实施阶段；
- Resource Exchange 等非目标没有混入施工范围。

## 4. 阶段 1：服务端与持久化骨架

### 工作

- [x] 增加服务端 TypeScript 构建配置；
- [x] 实现运行环境变量校验；
- [x] 实现 Desktop Origin server；
- [x] 实现 App Origin server；
- [x] 提供 `/health`；
- [x] 实现 Bearer admin token 中间件；
- [x] 实现原子 JSON 状态存储；
- [x] 实现数据目录初始化；
- [x] 实现单进程应用操作锁；
- [x] 把前端生产文件改由 Node server 提供；
- [x] 更新 Dockerfile 和 Compose 示例；
- [x] 增加服务端单元与 HTTP 集成测试。

### 出口条件

- 一个容器启动两个 origin；
- Desktop Origin 可以加载现有桌面；
- App Origin 不提供桌面或管理 API；
- 未授权请求不能调用管理 API；
- `/data` 重启后状态仍存在；
- V0.1 前端测试和构建不回归。

## 5. 阶段 2：注册表边界收紧

### 工作

- [x] 将 internal 收敛为编译期 app ID → React 组件映射；
- [x] 传统 `apps.json` 禁止 `kind: internal`；
- [x] 保留传统 iframe 和 external；
- [x] 定义 installed APP 投影类型；
- [x] 实现 built-in、legacy、installed 三来源合并；
- [x] 拒绝跨来源重复 ID；
- [x] `/api/v1/apps` 返回 active installed 投影；
- [x] 桌面 bootstrap 加载 managed apps；
- [x] API 失败时保留 built-in 和 legacy；
- [x] 增加冲突、降级和 session reconcile 测试。

### 出口条件

- 运行时数据不能创建或映射 internal；
- 管理服务不可用时桌面仍可使用；
- installed 应用只能成为 iframe；
- 重复 ID 不会覆盖已有应用；
- 已删除应用的持久窗口状态得到清理。

## 6. 阶段 3：检查与首次安装

### 工作

- [x] 定义 `GitHubSource` 接口；
- [x] 实现 GitHub URL 规范化；
- [x] 实现 ref → commit SHA；
- [x] 实现 archive 限制下载；
- [x] 实现安全解包和 staging；
- [x] 集成 Manifest JSON Schema；
- [x] 校验协议版本与原文；
- [x] 校验 `index.html`、LICENSE 和 icon；
- [x] 校验 owner、app ID 和冲突；
- [x] 实现 inspection 过期清理；
- [x] 实现配置合并和校验；
- [x] 实现首次安装落盘；
- [x] 实现 APP 记录最后提交；
- [x] 实现 App Origin 静态文件和配置路由；
- [x] 增加恶意路径、超限和失败原子性测试。

### 出口条件

- 本地 fixture 可以完成 inspect → install → GET index；
- 非法仓库不会产生 APP 记录；
- 安装过程没有执行仓库代码；
- commit SHA 被保存并用于入口 URL；
- 配置可以从 `.biunivers/config.json` 读取；
- GitHub 下线不影响已安装 fixture 运行。

## 7. 阶段 4：桌面应用管理 UI

### 工作

- [x] 设置应用增加“应用管理”区域；
- [x] 实现管理员 token 内存解锁；
- [x] 实现 GitHub URL 和 ref 表单；
- [x] 展示检查进度和具体错误；
- [x] 展示应用身份、来源、版本和 commit；
- [x] 根据配置 schema 生成字段；
- [x] 明确显示“配置会暴露给浏览器”；
- [x] 确认后安装；
- [x] 安装成功刷新 registry；
- [x] 展示 active/disabled 应用列表；
- [x] 增加键盘、加载、错误和空状态测试。

### 出口条件

- 管理员不使用命令行即可安装最小 fixture；
- 检查与安装是两个明确步骤；
- 必填配置、默认值和类型正确；
- token 不进入 localStorage；
- 安装成功后无需刷新整页即可看到应用；
- 设置窗口关闭后管理 token 消失。

## 8. 阶段 5：生命周期

### 配置

- [x] 修改配置；
- [x] 服务端重新校验；
- [x] 原子保存配置；
- [x] 提示重新打开应用生效。

### 更新

- [x] 检查新的 ref；
- [x] 确认 app ID 不变；
- [x] 用新 schema 迁移或重新填写配置；
- [x] 安装新版本目录；
- [x] 成功后切换 APP 记录；
- [x] 更新失败保留旧版；
- [x] 不强制替换现有 iframe。

### 停用和启用

- [x] active → disabled；
- [x] 关闭或提示关闭窗口；
- [x] 从 APP 表移除；
- [x] 不访问 GitHub即可重新启用。

### 卸载

- [x] 二次确认；
- [x] 阻止新窗口；
- [x] 从 APP 表移除；
- [x] 文件和配置进入清理；
- [x] 明示浏览器数据清理限制。

### 出口条件

- 四类生命周期操作均有自动化测试；
- 失败不会留下指向不存在入口的 APP；
- 更新失败后旧版仍能打开；
- disabled 应用重启后保持 disabled；
- 卸载后服务重启不会恢复应用。

## 9. 阶段 6：集成与发布准备

### 自动化

- [x] 完整 lint；
- [x] 完整单元测试；
- [x] 服务端集成测试；
- [x] 现有桌面 E2E；
- [x] 新增安装管理 E2E；
- [x] 生产构建；
- [x] Docker 镜像构建；
- [x] 容器重启持久化测试；
- [x] 依赖审计。

### 文档

- [x] 更新根 README；
- [x] 更新 Docker 和反向代理示例；
- [x] 更新应用管理操作说明；
- [x] 对照实现修订协议草案；
- [x] 同步 Manifest Schema 和开发模板；
- [x] 生成协议最终摘要候选；
- [x] 记录已知限制。

### 人工验收

达到此阶段时邀请用户参与：

- [x] 启动正式 Docker 镜像；
- [x] 从真实公开 GitHub 仓库安装最小示例；
- [x] 安装一个真实计算器应用；
- [x] 验证配置、窗口和重启；
- [x] 验证失败更新；
- [x] 验证停用、启用和卸载。

### 出口条件

- V0.2 需求中的 A–E 验收场景全部通过；
- 开发者依据开发包可以独立发布应用；
- 人工 Docker 验收通过；
- 没有阻断发布的已知缺陷；
- 决定协议继续草案或正式冻结；
- 更新文档状态并准备里程碑合并。

## 10. 测试矩阵

| 能力 | 单元 | 服务端集成 | 前端组件 | E2E |
|---|---:|---:|---:|---:|
| Manifest 校验 | ✓ | ✓ |  |  |
| GitHub URL/ref | ✓ | ✓ |  |  |
| 安装原子性 | ✓ | ✓ |  | ✓ |
| internal 边界 | ✓ |  | ✓ | ✓ |
| 配置表单 | ✓ | ✓ | ✓ | ✓ |
| App Origin 文件 |  | ✓ |  | ✓ |
| 更新回退 | ✓ | ✓ | ✓ | ✓ |
| 停用/启用 | ✓ | ✓ | ✓ | ✓ |
| 卸载 | ✓ | ✓ | ✓ | ✓ |
| 数据卷重启 |  | ✓ |  | ✓ |
| V0.1 窗口回归 | ✓ |  | ✓ | ✓ |

## 11. 风险和控制

| 风险 | 控制 |
|---|---|
| GitHub API 限额或网络波动 | 可选服务端 token、明确错误、本地 fixture 测试 |
| archive 路径攻击 | 限定 host、流式上限、路径规范化、拒绝链接 |
| 安装半成品进入桌面 | staging、最后写 APP 记录 |
| 更新破坏当前版本 | 新目录安装、成功后原子切换 |
| 管理 token 被第三方读取 | 双 origin、token 仅 Desktop Origin 内存 |
| 配置被误当 secret | 协议限制、UI 警告、无 secret 类型 |
| 协议与代码漂移 | Schema/模板/实现共享 fixture 和一致性测试 |
| 服务端扩大部署复杂度 | 单进程、JSON 状态、单容器和单数据卷 |
| 范围扩张到插件平台 | 坚守非目标，资源交换单独立项 |

## 12. 完工定义

只有同时满足以下条件才标记 V0.2 施工完成：

- 所有阶段出口条件通过；
- 主链路和失败链路都有测试；
- 用户完成 Docker 人工验收；
- 协议、Schema、模板、README 和实现一致；
- 完成的阶段在本文档中标记并留下验证记录；
- 合并前工作区干净且远端 CI 通过。

## 13. 施工记录

### 2026-07-28：阶段 1 与阶段 2 实现

- 新增 Node.js 服务端构建；
- Desktop Origin 与 App Origin 使用独立端口；
- 管理 API 使用 Bearer token；
- 安装状态使用原子 JSON 文件和单进程操作锁；
- 生产容器改为 Node 单容器和 `/data` 数据卷；
- internal 改为编译期 app ID 映射；
- 传统配置明确拒绝 internal；
- 桌面并行加载 legacy 和 managed APP；
- managed API 失败时保留 built-in 和 legacy；
- 本地双 origin 运行检查通过；
- lint、单元测试和生产构建通过。

当时执行环境无权访问 `/var/run/docker.sock`，因此暂缓 Docker 验证；该项随后由用户在阶段 6
完成镜像构建、双 origin 健康检查和数据卷重启验收。

### 2026-07-28：阶段 3 后端纵向链路

- 集成开发者包中的 Manifest Schema 和协议原文；
- 实现 GitHub URL 限制、ref 解析、受限 archive 下载和安全解包；
- 实现 30 分钟 inspection；
- 实现 manifest、协议、owner、入口、许可证和 icon 校验；
- 实现公开配置默认值、类型、范围和未知 key 校验；
- 实现 APP 记录最后提交的首次安装；
- 实现 App Origin 入口、资源和虚拟配置路由；
- 阻止 App Origin 暴露 manifest、协议文档、dotfile 和未登记版本；
- 使用本地 fixture 完成 inspect → install → iframe 投影 → GET index/config；
- 16 个测试文件、45 项测试、lint、生产构建和依赖审计通过。

阶段 3 尚未经过真实 GitHub 仓库和 Docker 环境人工验收；该验收保留到阶段 6。

### 2026-07-28：阶段 4 与阶段 5

- 设置应用新增管理员 token 内存解锁；
- 实现 GitHub URL/ref 检查和结构化错误展示；
- 根据 manifest 生成 string、boolean、integer、number 和 select 配置字段；
- 安装成功后动态刷新桌面注册表；
- 实现已安装应用列表；
- 实现配置修改、更新、停用、启用和卸载 UI 与 API；
- 停用和卸载会关闭对应窗口并刷新 registry；
- 更新保留旧 commit 文件，使已经打开的旧窗口不会立即失去资源；
- 公开配置直接保存在原子状态记录中，由 App Origin 虚拟路由生成；
- 生命周期集成测试覆盖配置、状态、更新、旧版本访问和卸载；
- 17 个测试文件、49 项测试、2 项浏览器 E2E、lint、生产构建和依赖审计通过。

阶段 4/5 仍需在真实浏览器和 Docker 环境完成视觉与端到端人工验收。

### 2026-07-28：阶段 6 首轮 Docker 验收

- `biunivers:v0.2-dev` 镜像构建成功；
- 新容器接管宿主机 `8080` 和 `8081`；
- Desktop Origin 与 App Origin 的 `/health` 均返回 `{"status":"ok"}`；
- 服务日志确认两个 origin 分别监听对应端口；
- README 中的手动运行示例已包含两个必需 origin，和本次有效启动参数一致。

浏览器人工验收通过：

- 桌面和设置窗口正常显示；
- 应用管理接受管理员 token 并成功解锁；
- 空的已安装应用列表和 GitHub 应用检查表单正常显示；
- 设置窗口的最小化、最大化、还原和关闭均正常。

### 2026-07-28：真实 GitHub 应用生命周期验收

- 创建公开示例仓库 `echo983/biunivers-example-app`；
- 从真实 GitHub `main` ref 完成检查和首次安装；
- GitHub URL 规范化、commit 固定、协议原文、Manifest、owner 和静态文件校验通过；
- App Origin 的入口、图标和虚拟公开配置可访问；
- 桌面能够打开示例应用，配置、计数交互和窗口控制正常；
- 容器重启后应用和配置从数据卷恢复；
- 配置修改后应用读取新值；
- 停用关闭窗口并移除入口，启用恢复入口；
- 从 `1.0.0` 更新到 `1.1.0`，已有配置保留；
- 协议被篡改的更新分支被拒绝，当前 `1.1.0` 不受影响；
- 无效 ref 最初错误映射为 500；修复后返回可操作的 GitHub ref 错误；
- 卸载关闭窗口并移除入口，容器重启后应用没有恢复；
- 公开注册表在卸载后返回空列表。

本轮人工验收覆盖安装、配置、重启、成功更新、失败更新回退、停用、启用和卸载。

### 2026-07-28：应用管理浏览器自动化

- 新增应用管理 Playwright E2E；
- 覆盖管理员 token 解锁；
- 覆盖无效 ref 的结构化错误展示；
- 覆盖检查仓库、配置表单和确认安装；
- 覆盖安装完成后的桌面 registry 动态刷新。

### 2026-07-29：独立计算器应用与施工收口

- 仅依据第三方开发者交付包创建公开仓库 `echo983/biunivers-calculator`；
- 应用不依赖宿主源码、服务端、构建步骤或第三方运行依赖；
- Manifest、owner、协议原文和必需文件通过宿主同源校验；
- 4 项计算核心测试覆盖优先级、括号、正负号、非法输入、除零和精度；
- 独立 Chromium 验证按钮、键盘和错误反馈；
- 从真实 GitHub `main` ref 安装并固定 commit；
- integer 和 select 配置通过虚拟配置路由生效；
- 用户确认运算、视觉和全部窗口控制正常；
- README 补充应用管理、双 origin 反向代理和已知限制；
- Static App Protocol v1 保持发布候选草案，随 V0.2 里程碑合并冻结正文。

V0.2 阶段 0–6 的施工、自动化和人工验收均已完成，进入合并评审。
